# model/saved/ — artifact provenance

- `evaluation_chart_DUMMY_DATA_semester1.png` — generated from
  `data/generate_dummy_data.py` synthetic data during Semester-1 development.
  **Not a Semester-2 result.** Do not cite this in the report as evidence of
  real-data performance. Delete once a real chart is generated by
  `model/evaluate.py` against `data/collected/metrics.csv` with >=1,000 rows.
- `proximascale_lstm.h5`, `proximascale_lstm.keras`,
  `proximascale_lstm_univariate.h5`, `scaler.pkl`, `scaler_univariate.pkl`,
  `proximascale_prophet.pkl` — committed model/scaler artifacts. Provenance
  (trained on real 81-row metrics.csv vs. metrics_dummy_backup.csv) is
  **not currently recorded**. Whoever retrains against the real dataset
  should note here which data produced the committed artifacts, and when.
