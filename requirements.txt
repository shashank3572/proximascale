# ProximaScale — all dependencies pinned
# Requires Python 3.11-3.13 (numpy 2.4 / pandas 3 / scikit-learn 1.8 / shap 0.51 need >=3.11).
# Verified: fresh venv on Python 3.12, `pip check` clean, full test suite passes.
# Saved model artifacts in model/saved/ were produced with this stack (sklearn 1.8.0,
# pandas 3, Keras 3) -- changing these pins can make them fail to load.

# LSTM model (Person B)
tensorflow==2.21.0
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
matplotlib==3.8.4
joblib==1.4.2
prophet==1.3.0
pyarrow==25.0.1         # needed to unpickle model/saved/proximascale_prophet.pkl (pandas 3)
shap==0.51.0

# App + Monitoring (Person A)
flask==3.1.3
psutil==5.9.8
locust==2.43.4
requests==2.34.2

# Dashboard (Person D)
streamlit==1.63.0
plotly==7.1.0
pyyaml==6.0.1
docker==7.2.0

# Testing / CI
pytest==9.1.1
flake8==7.3.0