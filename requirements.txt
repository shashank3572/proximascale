# ProximaScale — feature/app-monitoring
# Pin versions for reproducibility across all team members' machines
#
# Scope: this file covers ONLY the Flask app + monitoring module owned by
# Person A. Do not add ml/pandas/scikit-learn style deps here — those
# belong to Person B's model/ module and are pinned separately there.

Flask==3.1.3
docker==7.2.0
locust==2.43.4
requests==2.34.2
